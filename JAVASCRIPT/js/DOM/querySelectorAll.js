// Selectores del DOM->  query Selector All

const heading3 = document.querySelector(".heading")
console.log(heading3.textContent)

// retorna mas de 1 elemento
const enlaces = document.querySelectorAll(".navegacion a")
console.log(enlaces)
