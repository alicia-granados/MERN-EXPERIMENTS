// Template strings

const producto = " Tabñet de 12  pulgadas"
const precio = 400
const marca = "orange"

const textoFuncion = () => "este texto proviene de una función"

console.log(`${producto} $${precio} dolares ,marca ${marca}, ${textoFuncion()}`)