// Condicionales -> IF

const disponible = 4000
const retirar = 200

if(disponible > retirar){
    //se cumple la condición y se ejecuta el código 
    console.log("si puedes retirar")
}else{
    // no se cumpla la condición, se ejecuta este código
    console.log("lo siento intente más tarde")
}

